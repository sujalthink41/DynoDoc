# Normalize line endings to LF in the repo (consistent across Windows/macOS/Linux).
* text=auto eol=lf

# Treat these as binary (never normalize).
*.png binary
*.jpg binary
*.svg text eol=lf
*.lock text eol=lf
